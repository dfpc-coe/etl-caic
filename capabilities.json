{
    "version": "1.0",
    "name": "Colorado Avalanche Information Center",
    "description": "Ingests avalanche forecast zones and observations from the Colorado Avalanche Information Center (CAIC) and posts them to the map.",
    "compute": {
        "memory": 512,
        "timeout": 60
    },
    "permissions": [
        {
            "resource": "feature:*",
            "required": true,
            "description": "CAIC posts avalanche forecast zones and observations to the map"
        }
    ],
    "invocations": {
        "incoming": {
            "schedule": {
                "description": "Polls the Colorado Avalanche Information Center for updated forecasts on a schedule.",
                "default": {
                    "enabled": true,
                    "schedule": "rate(5 minutes)"
                }
            }
        }
    }
}
